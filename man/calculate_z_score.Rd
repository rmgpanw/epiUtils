% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistical_utilities.R
\name{calculate_z_score}
\alias{calculate_z_score}
\title{Calculate Z-Score from Effect Size and Standard Error}
\usage{
calculate_z_score(beta, se)
}
\arguments{
\item{beta}{Numeric vector of effect sizes (e.g., log odds ratios, regression coefficients)}

\item{se}{Numeric vector of standard errors (positive values)}
}
\value{
Numeric vector of Z-scores
}
\description{
Calculates the Z-score (standardized effect size) using the formula:
Z = beta / SE
}
\details{
This is commonly used in statistical testing to assess the significance
of an effect relative to its uncertainty.

The Z-score represents how many standard errors the effect size is away
from zero. Larger absolute Z-scores indicate stronger evidence against
the null hypothesis of no effect.

\strong{Requirements:}
\itemize{
\item \code{se} values must be positive
\item \code{beta} and \code{se} must have the same length or one must be length 1
}
}
\examples{
# Single effect size
calculate_z_score(beta = 0.5, se = 0.2)

# Multiple effect sizes
calculate_z_score(beta = c(-0.3, 0.5, 1.2), se = c(0.15, 0.2, 0.4))

}
\seealso{
\code{\link[=calculate_z_score_from_p]{calculate_z_score_from_p()}}, \code{\link[=calculate_se_from_z]{calculate_se_from_z()}}
}
