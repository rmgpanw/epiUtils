% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistical_utilities.R
\name{calculate_f_statistic}
\alias{calculate_f_statistic}
\title{Calculate F-Statistic}
\usage{
calculate_f_statistic(beta, se)
}
\arguments{
\item{beta}{Numeric vector of effect sizes}

\item{se}{Numeric vector of standard errors (positive values)}
}
\value{
Numeric vector of F-statistics
}
\description{
Calculates the F-statistic using the formula:
F = (beta / SE)^2
}
\details{
This is equivalent to Z^2 and is commonly used in ANOVA and regression
analysis to test the significance of effects.

The F-statistic is the square of the Z-score and follows an F-distribution
with 1 degree of freedom in the numerator. It's always non-negative and
larger values indicate stronger evidence against the null hypothesis.

\strong{Requirements:}
\itemize{
\item \code{se} values must be positive
\item \code{beta} and \code{se} must have the same length or one must be length 1
}

\strong{Relationship to other statistics:}
\itemize{
\item F = Z^2
\item F follows an F(1, df) distribution for simple effects
\item Under the null hypothesis, F follows a chi-squared distribution with 1 df
}
}
\examples{
# Single effect size
calculate_f_statistic(beta = 0.5, se = 0.2)

# Multiple effect sizes
calculate_f_statistic(beta = c(-0.3, 0.5, 1.2), se = c(0.15, 0.2, 0.4))

# Verify relationship with Z-score
beta <- 0.5
se <- 0.2
z <- calculate_z_score(beta, se)
f <- calculate_f_statistic(beta, se)
all.equal(f, z^2)  # Should be TRUE

}
\seealso{
\code{\link[=calculate_z_score]{calculate_z_score()}}
}
