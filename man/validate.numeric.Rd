% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate.numeric}
\alias{validate.numeric}
\title{Validate Probability Values}
\usage{
\method{validate}{numeric}(x, arg_name = "x", allow_zero = TRUE, allow_one = TRUE, ...)
}
\arguments{
\item{x}{Numeric vector of probability values}

\item{arg_name}{Character string naming the argument being validated}

\item{allow_zero}{Logical. If TRUE (default), allows probability values of 0}

\item{allow_one}{Logical. If TRUE (default), allows probability values of 1}

\item{...}{Additional arguments (currently unused)}
}
\value{
Returns \code{x} invisibly if validation passes
}
\description{
Validates that probability values are between 0 and 1 (inclusive).
Provides helpful error messages suggesting common issues.
}
\details{
This function checks that all values in \code{x} are between 0 and 1. If values
are outside this range, it provides helpful error messages suggesting
possible causes:
\itemize{
\item Values > 1 might be percentages that need to be divided by 100
\item Very small values (< 1e-10) might be -log10(p-values) that need conversion
\item Very large values might be chi-squared statistics or similar
}
}
\examples{
# Valid probability values
validate(c(0.05, 0.1, 0.5), arg_name = "p_values")

# This would throw an error:
# validate(c(1.5, 2.0), arg_name = "p_values")

}
